import 'package:flutter/material.dart';

class FantasyBody extends StatelessWidget {
  const FantasyBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Fantasy"),);
  }
}